import { NextResponse } from "next/server";

/**
 * API route pour les vidéos
 * TODO: Implémenter les endpoints nécessaires
 */
export async function GET() {
  return NextResponse.json({ message: "API videos endpoint" });
}

